description: "Simple mutation works"
schema: |
  type Film {
    title: String
  }
operations:
  - params:
      query: |
        mutation {
          createFilm(input: {title: "Hackers"}) {
            title
          }
        }
    response:
      data:
        createFilm:
          title: "Hackers"
